/* global angular */

require('angular');

angular.module('app', []);

var a = 1;

var Config = require('./core/config.js');

Config.pew();

const D = '4';

console.log('load', D);

if (1) {
    
}
else {
    
}